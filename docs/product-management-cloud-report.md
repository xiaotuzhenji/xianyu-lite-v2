# 商品管理云端验证记录

验证时间：2026-06-30

## 已完成的云端验证

- 云端仓库已更新到提交 `33d2907 fix: 云端验证复用已存在基础镜像`。
- 已清理云端 Git remote 中的历史明文 Token 地址，改为普通 GitHub HTTPS 地址。
- 已通过 SSH bundle 方式同步代码到云服务器，避免依赖服务器直连 GitHub。
- 已执行 `sh scripts/cloud-product-check.sh`。
- 后端测试镜像构建成功。
- 后端规则测试阶段随脚本完成，脚本未在测试阶段中断。
- `docker compose up -d --build` 已完成，`backend`、`frontend`、`mysql`、`redis`、`scheduler`、`websocket` 均为运行状态。
- 后端健康检查返回 `{"success":true,"message":"running"}`。
- 前端首页 HTTP 状态为 `200`。
- 使用默认管理员登录后，请求 `/api/v1/items` 成功返回 JSON，商品列表接口可用。
- 已补充账号归属隔离规则，避免商品管理页看到或操作其他用户账号。
- 已执行临时商品 API 流程并自动清理：创建临时账号、创建草稿商品、验证标题 30 字限制、验证新建商品不能伪造成已上架、验证负价格被拒绝、更新商品、保存并读取商品级发货配置、删除临时商品和临时账号。
- 已补充草稿商品 ID 变更时的发货配置迁移规则，避免发布后配置仍挂在旧草稿 ID 上。

## 云端处理过的问题

- 云服务器无法稳定直连 GitHub，改用本地 Git bundle 通过 SSH 同步。
- 原 `dockerproxy.com` 基础镜像源超时，已切换为 `docker.m.daocloud.io`。
- 云端磁盘曾达到 100%，已清理 Docker 构建缓存和未使用镜像，未删除数据卷。
- DaoCloud 镜像源偶发 TLS 超时，验证脚本已增加基础镜像拉取重试，并支持复用本地已有镜像。
- 云端临时流程曾发现账号列表因为 `last_active_at` 类型不匹配返回 500，已修复并重新验证通过。

## 尚需人工确认

以下流程涉及真实闲鱼账号、Cookie、商品和平台发布动作，未在自动脚本中强行执行：

- 在浏览器中扫码登录账号。
- 同步真实闲鱼商品，确认不会生成多余草稿。
- 新建草稿并上传多图，确认最多 8 张限制。
- 发布草稿到闲鱼，确认真实商品 ID 回写。
- 编辑已上架商品后重新发布，确认旧商品下架或显示明确失败原因。
- 为具体商品配置发货内容，确认不同商品互不覆盖。


## 2026-06-30 商品管理复验

- 云端代码已同步到 `8824e9d fix: 完善商品新建和筛选规则`。
- 云端已重新构建前端镜像，并重启 `backend`、`frontend`、`scheduler`、`websocket`。
- 云端服务状态复验：`backend`、`frontend`、`mysql`、`redis`、`scheduler`、`websocket` 均为运行中。
- 云端健康检查复验：`GET /health` 返回 `{"success":true,"message":"running"}`，前端首页 HTTP 状态为 `200`。
- 云端容器内规则测试复验通过：`test_item_flow.py`、`test_item_rules.py`、`test_publish_rules.py`、`test_delivery_rules.py`、`test_api_smoke.py`。
- 云端 HTTP 临时数据流程复验通过：登录默认管理员、创建临时账号、拒绝手动指定商品 ID、新建草稿、标题 30 字截断、负价格拒绝、超过 8 图拒绝、更新多图、保存并读取商品级发货配置、商品列表按账号读取、删除商品并清理发货配置、删除临时账号。
- 已修复账号筛选时商品列表只取默认 20 条的问题，前端现在按账号筛选也请求 `page_size=100`。
- 已修复新建商品接口仍可传手动 `item_id` 的问题，避免草稿被误当成真实已上架商品进入重发流程。
