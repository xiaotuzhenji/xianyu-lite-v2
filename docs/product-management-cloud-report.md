# 商品管理云端验证记录

验证时间：2026-06-30

## 已完成的云端验证

- 云端仓库已更新到提交 `33d2907 fix: 云端验证复用已存在基础镜像`。
- 已清理云端 Git remote 中的历史明文 Token 地址，改为普通 GitHub HTTPS 地址。
- 已通过 SSH bundle 方式同步代码到云服务器，避免依赖服务器直连 GitHub。
- 已执行 `sh scripts/cloud-product-check.sh`。
- 后端测试镜像构建成功。
- 后端规则测试阶段随脚本完成，脚本未在测试阶段中断。
- `docker compose up -d --build` 已完成，`backend`、`frontend`、`mysql`、`redis`、`scheduler`、`websocket` 均为运行状态。
- 后端健康检查返回 `{"success":true,"message":"running"}`。
- 前端首页 HTTP 状态为 `200`。
- 使用默认管理员登录后，请求 `/api/v1/items` 成功返回 JSON，商品列表接口可用。
- 已补充账号归属隔离规则，避免商品管理页看到或操作其他用户账号。

## 云端处理过的问题

- 云服务器无法稳定直连 GitHub，改用本地 Git bundle 通过 SSH 同步。
- 原 `dockerproxy.com` 基础镜像源超时，已切换为 `docker.m.daocloud.io`。
- 云端磁盘曾达到 100%，已清理 Docker 构建缓存和未使用镜像，未删除数据卷。
- DaoCloud 镜像源偶发 TLS 超时，验证脚本已增加基础镜像拉取重试，并支持复用本地已有镜像。

## 尚需人工确认

以下流程涉及真实闲鱼账号、Cookie、商品和平台发布动作，未在自动脚本中强行执行：

- 在浏览器中扫码登录账号。
- 同步真实闲鱼商品，确认不会生成多余草稿。
- 新建草稿并上传多图，确认最多 8 张限制。
- 发布草稿到闲鱼，确认真实商品 ID 回写。
- 编辑已上架商品后重新发布，确认旧商品下架或显示明确失败原因。
- 为具体商品配置发货内容，确认不同商品互不覆盖。
